# 新建「AI 模型」管理页面

## 目标
独立管理模型的前端显示名称。API 模型名如 `gpt-image-2` 可自定义前端显示为「智能图片生成模型v2」。

## 实现步骤

### 1. 创建 migration
新建 `ai_models` 表：
- `id` - 主键
- `model_id` - string, API 模型标识 (如 gpt-image-2)，唯一
- `display_name` - string, 前端显示名称 (如 智能图片生成模型v2)
- `type` - enum: chat / image，模型类型
- `is_active` - boolean, 是否启用
- `timestamps`

### 2. 创建 Model
`App\Models\AiModel`

### 3. 创建 Filament Resource
`AiModelResource`，放在「图片生成」导航组下：
- 表单：model_id（只读）、display_name、type、is_active
- 列表：显示所有模型，支持搜索和筛选

### 4. 修改渠道「获取模型」按钮逻辑
获取模型后，自动将新模型同步写入 `ai_models` 表（不存在则创建，display_name 默认等于 model_id）。
