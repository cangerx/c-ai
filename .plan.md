# 重构登录弹窗 + 图片懒加载优化

## 参考设计分析
- 图2: 暗色主题弹窗，大圆角输入框，GitHub按钮突出
- 图3: 极简白色弹窗，输入框轻边框，底部第三方登录图标
- 图4: 深灰卡片，输入框无边框融入背景，蓝色渐变按钮

## 方案

### 1. 登录弹窗重构（融合图3+图4风格）
修改 `public/index.html` 中的 CSS + HTML：
- 弹窗卡片：`380px` 宽，`20px` 圆角，白色背景，内边距 `36px`
- 标题：居中显示，`22px` 粗体
- 输入框：浅灰背景 `#f5f5f5`，无边框，`12px` 圆角，`46px` 高度，focus 时出现蓝色边框
- 主按钮：全宽蓝色渐变（`#4f46e5` → `#6366f1`），`46px` 高，白色文字
- "注册账号"改为底部居中文字链接："还没有账号？注册"
- "忘记密码"放在密码框右下角，灰色小字
- 关闭按钮用 × 图标
- 添加 `-webkit-autofill` 防变色
- OAuth 按钮改为底部分隔线 + 图标按钮行（参考图3）

### 2. 图片展示优化（防止页面卡顿）
修改 `loadGalleryWall()` 函数：
- 图片元素使用 `data-src` 代替 `src`，先显示灰色骨架占位
- 使用 IntersectionObserver 监听，进入视口时才设置 `src`
- 保留 `loading="lazy"` + `decoding="async"` 双保险
- 限制最多渲染 20 张图片

### 修改文件
仅修改 `public/index.html`
